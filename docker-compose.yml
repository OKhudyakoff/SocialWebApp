services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - SECRET_KEY=your-secret-key-change-in-production
    restart: unless-stopped
    working_dir: /app

  frontend:
    build: ./frontend
    ports:
      - "8001:8001"
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      - backend
    restart: unless-stopped
    working_dir: /app

volumes:
  backend_data: